/// <reference types="cheerio" />
/// <reference types="node" />
import * as stream from 'stream';
export interface TransformerExtras {
    dirname: string;
}
export declare type Transformer = ($: CheerioStatic, extras: TransformerExtras) => Promise<void>;
export declare const transform: (...args: Transformer[]) => stream.Transform;
export default transform;
