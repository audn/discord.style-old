"use strict";
var gulp = require('gulp');
var _a = require('../src'), transform = _a.transform, htmlSrcset = _a.htmlSrcset, lqip = _a.lqip;
gulp.task('html', function () {
    gulp
        .src('src/**/*.html')
        .pipe(transform(htmlSrcset({
        width: [1, 720],
        format: ['webp', 'jpg'],
    }), lqip({ query: '.image', addStyles: true }), lqip({ query: '.palette', method: 'primarycolor', addStyles: true })))
        .pipe(gulp.dest('dist'));
});
gulp.task('images', function () {
    gulp.src('src/**/*.jpg').pipe(gulp.dest('dist'));
});
gulp.task('default', ['html', 'images']);
